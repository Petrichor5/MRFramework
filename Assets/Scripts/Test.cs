using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameModule.UIMgr.OpenPanel<WBP_Counter_MainUI>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
